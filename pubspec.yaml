name: pico
description: Featherweight, zero-boilerplate state management for Flutter.
version: 1.0.0
repository: https://github.com/likhinmn/pico

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
